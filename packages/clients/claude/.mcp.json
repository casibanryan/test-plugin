{
  "_comment": "GENERATED by packages/clients/scripts/generate.js from channels.json (client: claude, channel: bundled). Do not hand-edit — run `npm run clients:generate` instead. CI fails if this drifts from channels.json. No credential and no network: the two read-only greeting tools are served by a local process.",
  "mcpServers": {
    "pivotly-greeting": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/server/greeting-stdio.js"
      ]
    }
  }
}
