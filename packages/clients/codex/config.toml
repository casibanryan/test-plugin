# GENERATED by packages/clients/scripts/generate.js from channels.json (client: codex, channel: production). Do not hand-edit — run `npm run clients:generate` instead. CI fails if this drifts from channels.json. No credential is needed: the hub serves two read-only tools anonymously.

[mcp_servers.pivotly_hub]
url = "https://pivotly-hub.azurewebsites.net/mcp"

[mcp_servers.pivotly_hub.http_headers]
"x-pivotly-client" = "codex"
"x-pivotly-channel" = "production"
"x-pivotly-contract" = "0.3.1"
