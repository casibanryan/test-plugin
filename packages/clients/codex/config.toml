# GENERATED by packages/clients/scripts/generate.js from channels.json (client: codex, channel: bundled). Do not hand-edit — run `npm run clients:generate` instead. CI fails if this drifts from channels.json. No credential and no network: the two read-only greeting tools are served by a process inside this plugin.
#
# This server is a process Codex starts. `command`/`args` below assume you have
# this repository checked out; adjust the path if you moved it. Nothing is
# fetched and no port is opened.

[mcp_servers.pivotly_greeting]
command = "node"
args = ["packages/clients/axle/server/greeting-stdio.js"]
