{
  "_comment": "Ships with the plugin. Claude Code discovers this file automatically and resolves ${CLAUDE_PLUGIN_ROOT} to wherever the plugin was installed — which is a version-stamped cache directory, so the path is different for every version and must never be hardcoded.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/version-notice.js\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
