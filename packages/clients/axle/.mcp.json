{
  "_comment": "GENERATED by packages/clients/scripts/generate.js from channels.json (client: axle, channel: production). Do not hand-edit — run `npm run clients:generate` instead. CI fails if this drifts from channels.json. No credential is needed: the hub serves two read-only tools anonymously.",
  "mcpServers": {
    "pivotly-hub": {
      "type": "http",
      "url": "https://pivotly-hub.azurewebsites.net/mcp",
      "headers": {
        "x-pivotly-client": "axle",
        "x-pivotly-channel": "production",
        "x-pivotly-contract": "0.3.1"
      }
    }
  }
}
