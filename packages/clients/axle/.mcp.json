{
  "_comment": "GENERATED by scripts/autopatch.js from channels.json (channel: production). Do not hand-edit — run `npm run autopatch -w @pivotly/axle` instead, and note that CI fails if this file drifts from channels.json. The token is read from the PIVOTLY_MCP_TOKEN environment variable at launch and is never written into this file.",
  "mcpServers": {
    "pivotly-hub": {
      "type": "http",
      "url": "https://pivotly-hub.azurewebsites.net/mcp",
      "headers": {
        "authorization": "Bearer ${PIVOTLY_MCP_TOKEN}",
        "x-pivotly-channel": "production",
        "x-pivotly-contract": "0.2.0"
      }
    }
  }
}
