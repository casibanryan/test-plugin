{
  "_comment": "GENERATED by packages/clients/scripts/generate.js from channels.json (client: gemini, channel: bundled). Do not hand-edit — run `npm run clients:generate` instead. CI fails if this drifts from channels.json. No credential and no network: the two read-only greeting tools are served by a process inside this plugin.",
  "mcpServers": {
    "pivotly-greeting": {
      "command": "node",
      "args": [
        "packages/clients/axle/server/greeting-stdio.js"
      ]
    }
  }
}
