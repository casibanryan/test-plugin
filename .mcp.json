{
  "_comment": "Project-scoped MCP config for working ON this repo — NOT the config the plugin ships. That one is generated at packages/clients/claude/.mcp.json by `npm run clients:generate`. This one runs the CANONICAL server in packages/server, so a change to it takes effect here without regenerating anything, and working on the repo needs no port, no host and no deploy.",
  "mcpServers": {
    "pivotly-greeting": {
      "type": "stdio",
      "command": "node",
      "args": [
        "packages/server/greeting-stdio.js"
      ]
    }
  }
}
