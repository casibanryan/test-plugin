{
  "name": "greeting-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Local MCP server for the greeting plugin: say hello and ask how the day is going.",
  "main": "greeting-server.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node greeting-server.js",
    "selftest": "node greeting-server.js --selftest",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  }
}
